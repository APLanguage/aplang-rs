use(std) std::println

struct Person {
    val name: str,
    val age: u8
}

fn test_person(person: Person) {
    println(r##"Hello "## + person.name + r", age " + person.age)
}

fn new_newborn(name: str) -> Person {
    return
}

fn test_non_empty_return(name: str) {
    return 1u16
}

fn test_wrong_return(name: str) -> u8{
    return 1u16
}

fn main() {
    var e = 2u16
    var c = 1u16
    var test = new_newborn(r"a") + new_newborn(r"b")
    var test = new_newborn(r"a") + 1u16
    var test = 1u16 + new_newborn(r"b")
    var d1 = !1u16
    var a = c + (d = e) - 2u16 / 2u16
    println(r"Hello World!")
    val person = Person(r"Amejonah", 42u8)
    test_person(person)
    val test = person.a
    if(1u16) while(1u16) {}
    while(1u16) {}
}