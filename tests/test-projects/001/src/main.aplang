use(my-project) main::Person as Test
use(std) std::*

struct Person {
    val name: str,
    val age: u8
}

fn test_person(person: Test) {
    println(r##"Hello "## + person.name + r", age " + str(person.age))
}

fn main() {
    println(r"Hello World!")
    val person = Person(r"Amejonah", 42)
    test_person(person)
}